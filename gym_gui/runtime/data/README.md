# Runtime Data Cache

Artifacts generated while the application runs are stored here. Treat these
files as ephemeral caches that support faster reloads and offline usage.

Sub-directories:

- `toy_text/` – Snapshots produced by the toy-text adapters (e.g. FrozenLake).
