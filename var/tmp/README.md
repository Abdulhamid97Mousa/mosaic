# Temporary Scratch Space

Short-lived exports (e.g., frame captures or packaged episodes) can be staged
here before being moved elsewhere. The application does not expect anything in
this directory to persist across restarts.
